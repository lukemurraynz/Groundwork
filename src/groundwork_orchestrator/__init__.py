"""Groundwork orchestrator.

Deterministic execution against pre-authored, version-pinned automation. Never imports a model
library or the control plane — enforced mechanically by ``tests/unit/test_import_boundaries.py``.
"""
