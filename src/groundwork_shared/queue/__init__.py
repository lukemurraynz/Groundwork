"""Per-subscription execution serialisation, shared between the control plane and the orchestrator
(FR-045b) — see ``subscription_lease.py``'s own docstring for why it lives here rather than in
either service package."""
